{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TomlConfig",
  "description": "Schema for the TOML configuration of the agent.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "The schema URL for validation."
    },
    "default-base": {
    "type": "string",
      "description": "The default base for the agent."
    },
    "quiet-mode": {
      "type": "boolean",
      "description": "Whether the agent should run in quiet mode."
    },
    "plan": {
      "type": "object",
      "description": "Configuration for the planning phase.",
      "properties": {
        "judge-extra-prompt": {
          "type": "string",
          "description": "Extra prompt for the judge in the planning phase."
        },
        "planner-extra-prompt": {
          "type": "string",
          "description": "Extra prompt for the planner in the planning phase."
        }
      },
      "additionalProperties": false
    },
    "implement": {
      "type": "object",
      "description": "Configuration for the implementation phase.",
      "properties": {
        "extra-prompt": {
          "type": "string",
          "description": "Extra prompt for the implementation phase."
        },
        "judge-extra-prompt": {
          "type": "string",
          "description": "Extra prompt for the judge in the implementation phase."
        },
        "completion": {
          "type": "object",
          "description": "Configuration for the implementation completion phase.",
          "properties": {
            "judge-extra-prompt": {
              "type": "string",
              "description": "Extra prompt for the judge in the implementation completion phase."
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "post-implementation-hook-command": {
    "type": "string",
      "description": "Command to execute after each implementation phase."
    }
  },
  "additionalProperties": false
}
